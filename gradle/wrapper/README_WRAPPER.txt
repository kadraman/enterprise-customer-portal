This folder should contain the gradle-wrapper.jar provided by the Gradle wrapper.

To generate the missing jar on a machine with Gradle installed run:

  gradle wrapper

Or, if you have no Gradle installed, you can download the gradle-wrapper.jar from
https://services.gradle.org/distributions/ or copy it from another project that
uses the same Gradle version (8.4.1 in gradle-wrapper.properties).
